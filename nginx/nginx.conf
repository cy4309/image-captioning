server {
    listen 80;

    # ✅ 後端 API 代理，先處理 API 請求
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 前端靜態檔案
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
        # try_files $uri $uri/ /index.html;
    }
}